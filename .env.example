# Scripts read environment variables; export these in your shell or CI.
DYNAMODB_ENDPOINT=http://127.0.0.1:8000
AWS_REGION=us-east-1
DYNAMODB_TABLE_PREFIX=dynamodb-lib-test
# Real AWS only: set DYNAMODB_TEST_AWS=1, omit DYNAMODB_ENDPOINT,
# and provide AWS_PROFILE or standard AWS credentials outside this file.
